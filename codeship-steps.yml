# initial checkin
- type: parallel
  name: example_parallel_step
  steps:
    - service: codeship_example
      command: nosetests --with-coverage -vv -m='*py$' -w test
    - service: codeship_example
      command: ls config.xml 
- name: example_tagged_step
  tag: master
  service: codeship_example
  command: coverage report
  service: codeship_example
  command: coveralls
